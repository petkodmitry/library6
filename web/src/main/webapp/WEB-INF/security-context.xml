<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
             http://www.springframework.org/schema/beans/spring-beans.xsd
             http://www.springframework.org/schema/security
             http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true">
        <intercept-url pattern="/" access="permitAll"/>
        <!--<intercept-url pattern="/home" access="permitAll"/>-->
        <intercept-url pattern="/blacklist**" access="hasRole('true') and hasRole('TRUE') and hasRole('1') and hasRole('Y')"/>
        <form-login default-target-url="/main"
                    authentication-failure-url="/myOrders"
                    username-parameter="login"
                    password-parameter="password"
                    login-page="/login"
        />
        <logout logout-success-url="/login" />
        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <authentication-provider user-service-ref="authService"/>
    </authentication-manager>

    <beans:bean id="authService" class="com.petko.auth.AuthenticationService"/>
</beans:beans>
